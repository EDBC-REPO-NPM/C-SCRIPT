#ifndef FILE_CSCRIPT
#define FILE_CSCRIPT
 
using namespace std;

class Stream {

    private:

    public:

};

class File {

    private:

    public:

};

#endif 


    /*
    char buffer[1024];

    FILE* _file; _file = fopen("done","w");

    for( int i=100; i--; ){
        fprintf( _file, "hola mundo de mierda %i \n", i );
    }   fclose( _file );
    
    _file = fopen("done","r");
    while( !feof(_file) )
        cout << fgets( buffer, 1024, _file );
        cout << endl;
    */

    /*
    while( !feof(_file) )
        cout << (char)fgetc( _file );
        cout << endl;
    */

//  fclose( _file );